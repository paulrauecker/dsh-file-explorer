## 文件树与文件预览独立收起

- 两个窗口各自有「>」收起按钮：预览左边缘的「>」只关预览；文件树左边缘的「>」只收树（双窗口同时打开时位于树与预览的接缝处）
- 收起文件树时预览窗口保留，并自动靠到页面右边缘；聊天区留白只让出可见窗口的宽度
- 标题栏 × 只收文件树（预览保留）；双击聊天区仍同时收起两个窗口
- editor / status 状态移入共享 store

代码变更:473c0e8
