## 新增「系统文件浏览器」入口

- 标题栏 VS Code 按钮旁新增图标按钮：在系统文件管理器中打开当前选中项
- 选中目录 → 直接打开该目录；选中文件 → 在所在文件夹中定位并高亮（Windows 资源管理器 `/select`、macOS Finder `open -R`、Linux 打开父目录）；未选中 → 打开项目根目录
- 新增 `POST /plugins/file-explorer/open-folder` 路由（subprocess 优先，Windows 保留 `Start-Process explorer.exe` 回退）
- 修复 `fs.resolve` 返回 FsTarget 对象被当作字符串导致的 "path" 类型错误（改用 `fs.processPath`）
- 新增 node:test 冒烟测试 12 例（固化真实 fs 契约与三平台行为，`npm test` 可回归）

本版本同时包含原 0.1.4 未发布的改动：面板默认宽度收窄至 227px、切换项目时重置过期预览。

代码变更:042229d
