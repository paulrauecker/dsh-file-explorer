## 预览标签化与项目过滤

- 预览标签化：浏览器式多标签——每个文件一个标签，按打开顺序排列、点击后台标签不改变顺序，标签条溢出横向滚动，切换不重新加载，标签可单独关闭
- 预览面板「>」/「收起预览」= 隐藏面板、保留全部标签，点树中文件即恢复
- 双击聊天区：同时收起两个窗口，并按项目过滤——属于当前项目文件夹的标签保留、重新打开文件浏览器时自动恢复；不属于的关闭；切换项目/会话同样过滤
- 默认宽度调整：文件树 227 → 303px，预览面板 400 → 600px
- 构建迁移：源码迁入 `src/`（`tsc` + `tsdown` 生成 `lib/`），新增 build 脚本与开发依赖；host 行为不变

代码变更:8102e2d
